#include <iostream>
using namespace std;

int main(){
    // system("pause"); // only valid on windows cmd, not on linux shell
    const int a=1;
    cout << "a = " << (char)65 << endl;
    return 0;
}